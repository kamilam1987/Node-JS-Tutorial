//********************** MODULES AND REQUIRE *********************

//Require function(path to the module)
let counter = require('./count'); // Return and store in variable

console.log(counter(['kate', 'orla', 'marry']));
